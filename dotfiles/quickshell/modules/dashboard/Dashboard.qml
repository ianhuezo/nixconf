import Quickshell
import QtQuick

Item {
    implicitWidth: 200
    implicitHeight: 150
    Rectangle {
        implicitHeight: parent.implicitHeight
        implicitWidth: parent.implicitWidth
        anchors.fill: parent
        color: 'red'
    }
}
