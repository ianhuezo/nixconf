#pragma Singleton

import Quickshell
import QtQuick

Singleton {

    property list<var> jobs: [
        //queue name it's a part of
        //id of queue job
        //args from whatever is needed to run the job
        //
        {}
    ]

    function enqueue() {
    }
}
