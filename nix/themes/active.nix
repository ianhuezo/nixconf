# The active desktop theme.
#
# Imported by both home-manager (desktop colours) and the system RGB module
# (case lighting), so switching themes here moves both together rather than
# leaving the fans on whatever the last theme was.
import ./dark-ethereal
